@model IEnumerable<ekipage.Models.LessonContent>

@{
    ViewData["Title"] = "Index";
}

<h1>Lektionstyp</h1>
<!--
<p>
    <a asp-action="Create">Create New</a>
</p>
    -->
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lesson)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lesson)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.LessonContentId">Redigera</a>
                <!-- |
    <a asp-action="Details" asp-route-id="@item.LessonContentId">Details</a> |
    <a asp-action="Delete" asp-route-id="@item.LessonContentId">Delete</a>
    -->
            </td>
        </tr>
        }
    </tbody>
</table>
